# Lab Theme

Shared design tokens (easings, shadow-border ring, and keyframes) every moumenlab component builds on.

- Install: `npx moumenlab add lab-theme` — or `npx shadcn@latest add https://lab.moumen.dev/r/lab-theme.json`
